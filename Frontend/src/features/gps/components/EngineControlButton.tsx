export function EngineControlButton({ vehicleId }: { vehicleId: string }) {
  // Les endpoints REST GPS (coupure/rétablissement) ne sont pas encore câblés dans ce squelette.
  // On ne rend rien pour éviter des boutons qui ne fonctionnent pas.
  void vehicleId
  return null
}

